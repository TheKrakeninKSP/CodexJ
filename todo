[DONE] -- export db to encrypted dump
[DONE] -- import db from encrypted dump
[DONE]-- import entry from plaintext formatted dump
[DONE] -- add test for media upload

[DONE] -- media reference counting and management to stop media in use being deleted
[DONE] -- fix frontend
[DONE] -- test media is not being cleaned up
[DONE] -- inline journal/workspace creation button

[DONE] -- periodic job function to clean up media files that have 0 reference count
[DONE] -- export without user delete
[DONE] -- audio as a valid media type
[DONE] -- add ui option to import entries

[DONE] -- imported dump bungles the media refs, audios are not handled
[DONE] -- export also exports user name and password hash
[NVMD] -- audio m4a not being taken properly -- browser's codec compatibility issue with ALAC, not app-side issue
[DONE] -- add a custom field to entry that is a checkbox to show audio inline or in a separete cards

[DONE] -- delete should only be available in previleged mode
[DONE] -- journal/workspace/entry delete options
[DONE] -- frontend style sync - previleged mode options in a separate popup box maybe? 
[DONE] -- show version near CodexJ in sidebar

[DONE] -- user info box/helps
[DONE] -- search api improvements
[DONE] -- add build system
[DONE] -- add readme

---------------------------------------------------------------------------------------------------------- Release 0.3

[DONE] -- fullscreen toggle
[DONE] -- entry that is deleted moves to bin with orphan journal and workspace id, restore should prompt user for journal selection
[DONE] -- entry_type delete option
[DONE] -- entry_type specific to workspace, not user scope
[DONE] -- use nested workspace routes for entry_types API
[DONE] -- "Begin Writing" ghost text is incorrectly positioned
[DONE] -- help file not served properly in built app
[DONE] -- dark mode
[DONE] -- hyperlink entries
[DONE] -- add a flag called "show-urls-inline" set to false by default in the custom_metadata of the entry when importing webpage
[DONE] -- now that entry hyperlinking is present, should the back button in entryreader actually go back instead of to the home?
[DONE] -- link media type exceeds entry display width and overruns the entry reader

-- UI polishing
-- updated entries fail to save
-- comprehensive help page update

complex

-- global music registry for song lookup
-- why base64 encoding of media files? waste of space.

[PARTIAL] -- any way to add webpages? save them? -- added Single-File based page archiving
[PARTIAL] -- web-page extraction triggers bot-flagging on sites like reddit -- added route for importing manually archived Single-File htmls
[DONE] -- when adding a link, add the card with a placeholder and allow saving of entry and then update with the archived page asynchronously

minor
-- app exit button in UI
-- journal name in imported text file is useless
-- in the UI, when entering too long names for workspace, journal, entry name etc, a message "String should have at most XXX characters" is displayed. For entry this is unclear since user can think it applies to the body instead of the entry name or entry type.

----------------------------------------------------------------------------------------------------------
Release 1.0