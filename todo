[DONE] -- export db to encrypted dump
[DONE] -- import db from encrypted dump
[DONE]-- import entry from plaintext formatted dump

[DONE] -- add test for media upload
[DONE] -- media reference counting and management to stop media in use being deleted
[DONE] -- fix frontend
[DONE] -- test media is not being cleaned up
[DONE] -- inline journal/workspace creation button

-- in the UI, when entering too long names for workspace, journal, entry name etc, a message "String should have at most XXX characters" is displayed. For entry this is unclear since user can think it applies to the body instead of the entry name or entry type.
-- delete should only be available in previleged mode
-- journal/workspace/entry type delete options
-- add ui option to import entries
-- audio as a valid media type
-- global music registry for song lookup
-- periodic job function to clean up media files that have 0 reference count
-- export without user delete
-- export also exports user name and password hash